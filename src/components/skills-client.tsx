"use client";
import { FormEvent, useEffect, useState } from "react";

type Profile = { skills: Array<{ name: string; status: string; assessmentScore?: number; evidenceCount: number }> };
async function load() { const response = await fetch("/api/profile"); const body = await response.json(); if (!response.ok) throw new Error(body.error?.message ?? "Unable to load skills."); return body.profile as Profile; }
const label = (value: string) => value.replaceAll("_", " ").toLowerCase().replace(/\b\w/g, (letter) => letter.toUpperCase());
export default function SkillsClient() { const [profile, setProfile] = useState<Profile | null>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { void load().then(setProfile).catch((error: unknown) => setError(error instanceof Error ? error.message : "Unable to load skills.")); }, []); async function add(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/profile/skills", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name") }) }); const body = await response.json(); if (!response.ok) { setError(body.error?.message ?? "Unable to add skill."); return; } setProfile(body.profile); event.currentTarget.reset(); } async function remove(name: string) { const response = await fetch(`/api/profile/skills?name=${encodeURIComponent(name)}`, { method: "DELETE" }); const body = await response.json(); if (!response.ok) { setError(body.error?.message ?? "Unable to remove skill."); return; } setProfile(body.profile); } if (!profile) return <main className="mx-auto max-w-5xl px-6 py-10 text-sm text-stone-600">{error ?? "Loading skills…"}</main>; return <main className="mx-auto w-full max-w-5xl px-6 py-10"><p className="text-xs font-medium uppercase tracking-[0.18em] text-stone-500">Skills</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Claims with proof</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-stone-600">Self-claims remain claims. Only results and supporting evidence can change a verification status.</p>{error && <p className="mt-4 text-sm text-red-700">{error}</p>}<div className="mt-8 divide-y divide-stone-200 border-y border-stone-200">{profile.skills.map((skill) => <div key={skill.name} className="grid gap-2 py-5 sm:grid-cols-[1fr_auto_auto_auto] sm:items-center sm:gap-6"><p className="font-medium">{skill.name}</p><p className="text-sm text-stone-600">{skill.assessmentScore === undefined ? "No assessment" : `${skill.assessmentScore}%`}</p><p className="text-sm text-stone-600">{skill.evidenceCount} evidence item{skill.evidenceCount === 1 ? "" : "s"}</p><div className="flex items-center gap-4"><p className="text-sm font-medium">{label(skill.status)}</p><button type="button" onClick={() => void remove(skill.name)} className="text-sm underline">Remove</button></div></div>)}{profile.skills.length === 0 && <p className="py-5 text-sm text-stone-600">Add a skill to begin building a verifiable profile.</p>}</div><form onSubmit={add} className="mt-6 flex max-w-lg gap-3"><label className="sr-only" htmlFor="skill-name">Skill name</label><input id="skill-name" name="name" required minLength={2} placeholder="e.g. JavaScript" className="h-11 min-w-0 flex-1 border border-stone-300 bg-white px-3" /><button className="h-11 border border-stone-900 bg-stone-900 px-5 text-sm font-medium text-stone-50">Add skill</button></form></main>; }
